function processInput () {  
    var name = document.getElementById("name-id").value
    var email = document.getElementById("email-id").value
    var str = "Hello " + name + "    (" + email + ")"
    alert(str)
}